---
export interface Props {
    name: string;
}
---
<div class="flex items-center gap-3 bg-slate-800/50 p-3 rounded-lg">
    <div class="w-7 h-7">
        <slot /> <!-- Aquí se insertará el SVG del icono -->
    </div>
    <span class="font-medium text-slate-300">{Astro.props.name}</span>
</div>